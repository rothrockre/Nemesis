<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nemesis">
    
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".AddWorkoutPage"></activity>
        <activity android:name=".SignUp" />
        <!--<activity android:name=".sign_up" /> -->
        <activity android:name=".Cardio" />
        <activity android:name=".Profile" />
        <activity android:name=".Workout" />
        <activity android:name=".Homepage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="news.API_KEY"
            android:value="0b23857f53cb4d00aee3ad9e141222cd"/>

    </application>

</manifest>